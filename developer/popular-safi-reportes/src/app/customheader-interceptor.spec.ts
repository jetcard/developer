import { CustomheaderInterceptor } from './customheader-interceptor';

describe('CustomheaderInterceptor', () => {
  it('should create an instance', () => {
    expect(new CustomheaderInterceptor()).toBeTruthy();
  });
});
